import styled from "styled-components";

export const Styled = {
    Wrapper: styled.div`
        min-height: 100vh;
        background: #050509;
        color: #f4f4f4;
        font-family: Arial, Helvetica, sans-serif;
        overflow-x: hidden;

        a {
            color: inherit;
            text-decoration: none;
        }

        main {
            width: 100%;
            max-width: 1440px;
            margin: auto;
        }
    `,
};
